# Local dev: point at the standalone Express backend on port 8000.
# Production (Vercel): leave this unset — frontend and backend share an origin,
# so fetches go to /api/* on the current host automatically.
VITE_BACKEND_URL=http://localhost:8000
